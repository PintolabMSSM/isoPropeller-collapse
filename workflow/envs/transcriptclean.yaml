name: transcriptclean_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.8
  - pip
  - samtools >=1.12
  - pigz
  - pip:
      - git+https://github.com/mortazavilab/TranscriptClean.git
